# Jogoteca-Flask-App 🎮

A web application developed with Python and the Flask microframework to manage a collection of games.

## 🚀 Technologies
* Python 3
* Flask 2.0.2
* HTML5 / CSS3

## 🛠️ How to run the project
1. Clone the repository:
   `git clone https://github.com/YOUR_USERNAME/jogoteca-flask-app.git`
2. Create a virtual environment:
   `python -m venv venv`
3. Activate the environment:
   `.\venv\Scripts\activate` (Windows)
4. Install dependencies:
   `pip install -r requirements.txt`
5. Run the application:
   `python jogoteca.py`
